name: Test
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Display Rust version
        uses: ./.github/workflows/actions/echo
        env:
          XX: nihaowa
